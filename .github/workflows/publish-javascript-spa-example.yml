name: Publish javascript-spa-example

on:
  push:
    branches:
      - main
    paths:
      - javascript-spa-example/**
      - .github/workflows/publish-javascript-spa-example.yml
  pull_request:
    paths:
      - javascript-spa-example/**
      - .github/workflows/publish-javascript-spa-example.yml

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: ./javascript-spa-example
          push: ${{ github.event_name != 'pull_request' }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/javascript-spa-example:latest
            ghcr.io/${{ github.repository_owner }}/javascript-spa-example:sha-${{ github.sha }}
